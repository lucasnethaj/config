#!/bin/sh

on_exit() {
	for child in $(jobs -p); do
		jobs -p | grep -q $child && kill $child
	done
	# Extra paranoia
	[ -d "${state_path}" && -w "${state_path}" ] && rm -rf -- "${state_path}"
}

shotkey & 
dwmst &
picom &
wallpaper
dwm
