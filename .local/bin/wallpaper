#!/bin/bash
#Set at random wallpaper from ~/Pictures/wallpapers
imgdir="$HOME/Pictures/wallpapers"

r=$RANDOM
max=$(ls -l $imgdir | wc -l)
let "r %= max"
sel=$(find $imgdir | sed "${r}q;d")

feh --bg-fill -. "${sel}"
